Algoritmo "A0001"
  var res, cont, x, n: Inteiro
  inicio
     cont <- n, res <- 0, x <- 2, n <- 4
     Enquanto (cont>=n) faca
         res <- res * x
         cont <- cont + 1
     FimEnquanto
     escreva(res)
fimAlgoritmo